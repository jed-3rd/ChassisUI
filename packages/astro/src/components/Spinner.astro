---
export interface Props {
  size?: 'sm' | 'md' | 'lg';
}

const { size = 'md' } = Astro.props;
---

<chassis-spinner size={size}></chassis-spinner>

<script>
  import '@chassisui/core/spinner';
</script>
