---
export interface Props {
  value?: string;
  variant?: 'underline' | 'contained';
}

const { value = '', variant = 'underline' } = Astro.props;
---

<chassis-tabs value={value} variant={variant}>
  <slot />
</chassis-tabs>

<script>
  import '@chassisui/core/tabs';
</script>
