---
export interface Props {
  value?: string;
  multiple?: boolean;
}

const { value = '', multiple = false } = Astro.props;
---

<chassis-toggle-group value={value} multiple={multiple}>
  <slot />
</chassis-toggle-group>

<script>
  import '@chassisui/core/toggle-group';
</script>
